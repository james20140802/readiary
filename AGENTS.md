# Readiary 작업 안내

## 프로젝트와 제품 철학

Readiary는 마음에 남은 문장과 생각을 가볍게 기록하고, 시간이 지난 뒤 다시 읽도록 돕는 한국어 독서 일기 PWA다. 독서량을 경쟁시키기보다 **기록하는 행동의 비용을 낮추고, 다시 만나는 기록을 보상으로 만든다.**

- 기록 한 건은 한 번의 기록 순간이다. `quote`(책의 문장)와 `note`(나의 생각) 중 하나만 있어도 된다. 두 필드가 모두 있는 기록도 가능하다.
- 페이지 번호와 책의 총 쪽수는 선택 정보다. 전자책·오디오북처럼 쪽수를 모르는 독서도 가능해야 한다.
- 완독은 사용자가 선언한다. 진행률로 완독을 강제하거나 제한하지 않는다.
- 배지 대신 회상 카드, 주간 누적 기록, 월별 회고, 완독한 책의 발췌집으로 기록을 다시 만난다.
- 친구의 문장을 읽고 좋아요·댓글을 남기는 소셜 기능, 명시적으로 활성화하는 외부 공유가 있다.
- 부담을 주는 랭킹·연속 기록 경쟁보다 조용한 독서 습관과 개인의 서재를 지향한다.

### 실제 제품의 주요 공간

| 공간    | 사용자가 하는 일                                                      | 코드 입구                                       |
| ------- | --------------------------------------------------------------------- | ----------------------------------------------- |
| 랜딩    | 문장 기록과 회고의 가치를 체험하고 시작한다                           | `components/landing/`                           |
| 오늘    | 문장·생각을 남기고 지난 기록과 읽던 책을 다시 만난다                  | `app/protected/dashboard/`                      |
| 내 책장 | 책을 등록하고, 책등 또는 목록으로 찾아 기록·완독·발췌집으로 이동한다  | `app/protected/books/`, `components/books/`     |
| 소셜    | 친구를 찾고 문장 엽서를 읽고 반응한다                                 | `app/protected/social/`, `components/social/`   |
| 프로필  | 자신의 독서 이력을 한 권의 책처럼 펼쳐 보고 대표 문장·책갈피를 고른다 | `app/protected/profile/`, `components/profile/` |
| 알림    | 앱 안의 소식과 기기별 독서 알림 설정을 확인한다                       | `app/protected/notifications/`, `lib/push/`     |

## Design Context — 먹과 종이

**문장이 주인공. 책의 목소리는 부리, 나의 도구는 산세리프. 그림자 대신 헤어라인. 파랑은 잉크처럼 아껴서.**

기본 디자인 규범은 [UI Guidelines](docs/ui-guidelines.md), 런타임 토큰은 `app/globals.css` → `tailwind.config.js` → `components/ui/`로 이어진다. 새 화면도 이 문법과 기존 형제 화면을 따른다.

### 색과 서체

- 종이 바탕 `paper`, 표면 `card`/`card-raised`, 본문 `ink`, 보조 `ink-sub`/`ink-faint`, 경계 `hairline`/`hairline-strong`를 사용한다.
- 강조색은 파란 잉크로 확정됐다. 라이트 `#2D5FB8`, 다크 `#85A9EC`다. 주홍은 현재 대안이 아니다.
- 값은 토큰 정의에서 관리한다. 일반 UI에 임의의 hex나 `blue-500` 같은 원색 팔레트를 추가하지 않는다.
- 문장·책 제목·회고 카피는 `font-serif`(셀프호스팅 마루 부리), 버튼·라벨·내비게이션·메타데이터는 `font-sans`(셀프호스팅 Pretendard)다.
- `text-page-title`, `text-section-title`, `text-body`, `text-caption`, `text-quote`, `text-note`, `text-seal` 등 기존 크기 토큰을 우선 사용한다.
- 다크모드는 OS 설정을 따른다. 의미 토큰의 CSS 변수가 전환되므로 일반 컴포넌트에서 색을 `dark:`로 분기하지 않는다. 현재 앱 내 테마 토글은 없다.

### 표면과 상호작용 문법

- 일반 도구·폼·카드는 종이색과 가는 선으로 위계를 만든다. 카드 기본 곡률은 6px, 버튼과 칩은 알약형이다.
- `Button`의 주요 행동은 먹 배경, 보조 행동은 헤어라인 외곽선이다. 파란색을 모든 주요 버튼의 배경으로 쓰는 시스템이 아니다.
- 기록·책 등록·인증·프로필 수정은 상자를 늘리기보다 괘선형 입력을 쓴다. 공용 `Input`/`Textarea`의 `variant="line"`과 형제 화면을 참고한다.
- 책 상세의 기록은 기본적으로 테두리 카드의 반복보다 **원고 흐름**이다. 날짜·쪽수는 마진 노트, 문장과 생각은 부리 서체, 구분은 헤어라인이다.
- 읽기 진도는 게이지보다 `184 / 300` 같은 잉크 분수다. 쪽수가 없다는 이유로 기록이나 완독을 막지 않는다.
- 책등 서가, 펼치는 책, 발췌집, 소셜 엽서, 프로필 책은 제품의 특징이다. 일반 대시보드 카드로 일괄 치환하지 않는다.
- 아이콘은 `lucide-react`, 기본 선 굵기는 1.75다. 장식 이모지와 기울어진 도장 박스는 지양한다. `Seal`은 날짜·상태 라벨이다.
- 문구는 사용자가 할 행동과 결과를 말한다. 예: “남기기”, “생각 덧붙이기”, “완독”, “이 기기 알림 켜기”. 기능 설명도 구현 방식보다 독자가 얻는 경험을 먼저 설명한다.

### 현재 문서와 구현의 차이

아래는 새 디자인 결정이 아니라, 2026-09-08 기준 확인한 차이다.

- 가이드는 그림자 금지지만 `Slide`, `OpenBook`, 프로필 책, `.perforated` 엽서에는 깊이·그림자 효과가 있다. 이를 일반 카드의 기본값으로 확대하지 않는다. 기존 입체 표현을 없애거나 공식 예외로 확정하는 것은 별도의 디자인 결정이다.
- `ink-faint`의 다크 값은 가이드의 `#6E655A`와 달리 코드에서 `#8E8476`으로 조정되어 있다. 현재 렌더링 값을 확인할 때는 CSS를 읽는다.
- 초기 설계에는 홈 피드 프리뷰 제거가 있지만 현재 홈에는 `BoardWidget` 등이 있다. 초기 설계의 모든 항목을 현재 구현 완료 상태로 해석하지 않는다.
- README의 Next.js 14·배지 중심 설명은 현행 코드와 다르다. 현재 패키지는 Next.js 16, React 19다. 버전과 실행 명령은 `package.json` 및 lockfile을 확인한다.
- 노션의 이전 색상 후보·서체 후보와 초기 체크리스트에는 과거 상태가 남아 있다. 확정 결정과 현재 구현을 함께 확인한다.

## 작업 흐름 — 필수

### 1. 작업마다 별도 worktree

파일을 수정하기 전에 해당 목적의 브랜치와 worktree를 생성하고, 수정·설치·검증·커밋은 그 worktree에서 진행한다. 기본 checkout의 `main`에서 직접 구현하지 않는다. 이미 같은 작업용 worktree 안이라면 중복 생성하지 않는다.

시작할 때 현재 변경과 worktree 목록, 기준 브랜치를 확인한다. 예시:

```sh
git status --short
git worktree list
git fetch origin
git worktree add ../readiary-<작업명> -b <prefix>/<작업명> origin/main
```

### 2. 브랜치 prefix

목적을 드러내는 `feat/`, `fix/`, `chore/`, `docs/` 등을 사용한다. 예: `feat/reading-recap`, `fix/composer-draft`, `docs/project-guide`. 기본 prefix로 `codex/`를 사용하지 않는다.

### 3. 완료하면 Draft PR

작업을 완료하고 변경 범위에 맞는 검증을 한 뒤 커밋·push하고 **Draft PR**을 작성한다. 문서 작업도 포함한다. PR을 만들었다는 사실이 머지 또는 배포 완료를 뜻하지는 않는다.

PR 제목과 본문에는 **행동의 언어**를 포함한다. 기술 설명만 나열하지 말고 사용자 또는 운영자에게 무엇이 가능해졌는지, 어떤 불편이 줄었는지 먼저 쓴다. 내부 정리처럼 직접적인 사용자 변화가 없으면 그 사실과 유지보수자의 이점을 정확히 설명한다.

권장 본문 구성:

```md
## 무엇이 달라지나요

독자는 문장을 쓰다가 다른 책을 골라도 작성하던 내용을 이어서 남길 수 있습니다.

## 어떻게 바꾸었나요

책 선택을 화면 안에서 처리하고, 선택 전후 입력을 유지합니다.
필요한 구현 설명과 영향 범위를 여기에 덧붙입니다.

## 확인한 내용

- 확인한 시나리오와 결과
- 실행한 검사와 아직 확인하지 못한 항목
```

### 4. 머지 후 정리

PR의 머지가 확인되면 **그 PR의 원격 브랜치, 로컬 브랜치, 작업 worktree를 삭제**한다. Draft PR 작성 시점에는 검토할 브랜치와 worktree를 유지한다.

- 머지된 PR과 정확히 연결된 브랜치인지 확인한다. squash merge에서는 단순 ancestry 검사만으로 판단하지 않는다.
- 기본 checkout으로 이동하고 `main`을 fast-forward로 동기화한 뒤 작업 worktree를 정리한다.
- 미커밋 변경, 미추적 파일, 해당 worktree를 사용하는 작업·프로세스가 있으면 보존 여부를 확인한 뒤 정리한다. 다른 작업의 브랜치·worktree와 기존 `.codex/` 파일을 함께 삭제하지 않는다.
- 원격 브랜치가 이미 삭제됐으면 중복 삭제하지 않는다. 잠금이나 미병합 경고를 이유 확인 없이 강제로 해제하지 않는다.

## 품질과 운영 — 필수

### 1. UI 변경은 실제 사용 조건에서 검증

변경한 화면을 모바일 폭, 라이트·다크모드, 키보드로 확인한다. 로딩·빈 상태·오류·성공 상태 중 변경에 해당하는 상태를 검증하고, PR에 시나리오와 결과를 남긴다. iOS 설치형 PWA와 일반 브라우저의 결과를 구분한다. 실행하지 못한 조건은 미검증으로 명시하며 코드 검사나 데스크톱 에뮬레이션을 실기기 확인으로 표현하지 않는다.

### 2. 문서·토큰·구현의 일치

디자인 시스템 결정을 바꾸면 관련 가이드, 런타임 토큰, 공용 컴포넌트를 같은 변경에서 갱신한다. 과거 계획과 현재 구현이 다르면 차이와 근거를 명시한다. 현재 코드를 발견했다는 이유만으로 새로운 제품·디자인 결정이 승인되었다고 간주하지 않는다.

### 3. 완료 단계를 구분해서 보고

코드 완료, Draft PR 생성, 최신 head의 검사, 미해결 리뷰, 머지, 배포, DB 마이그레이션, 실기기 확인을 각각 구분한다. 해당하지 않는 단계는 제외하고, 남은 단계는 명시한다. 이전 커밋의 검사 결과나 CI 통과만으로 최신 코드의 머지 준비 또는 프로덕션 정상 동작을 단정하지 않는다.

### 4. 공개 범위와 비밀값 보호

개인정보·공유 동작을 바꾸면 UI, API, DB/RLS, Storage, PWA·브라우저 캐시와 운영 문서를 함께 검증한다. 친구 열람과 외부 링크 공개의 차이, 비공개 전환·공유 해제 후의 접근을 확인한다. 환경 파일·비밀키·인증 토큰을 임의로 worktree에 복사하거나 코드·문서·로그·PR에 노출하지 않는다. 필요한 환경 설정은 승인된 공급 경로를 사용한다.

### 5. 데이터 접근과 선택 UX

DB 집계·Supabase RPC·상한 있는 미리보기로 왕복과 전송량을 줄이고, 선택 후보는 검색·페이지네이션으로 조회한다. 닫힌 선택창을 위해 전체 기록을 미리 가져오지 않는다. 현재 선택을 먼저 보여주고 변경·해제·키보드 조작을 제공하며, 책과 문장 등 독립적인 선택 목록의 로딩·실패·재시도를 분리한다. 성능 최적화로 인증 확인이나 RLS를 우회하지 않는다.

## 코드 탐색과 실행

- Next.js App Router: `app/`; 재사용 UI: `components/`; 인증·데이터·도메인 로직: `lib/`; DB 변경: `supabase/migrations/`.
- Supabase Auth와 PostgreSQL/RLS를 사용한다. 브라우저 API 호출의 기존 진입점은 `lib/api/fetch.ts`의 `apiFetch`, 읽기 서버 인증은 `getServerUser`다.
- `401` 모두가 세션 만료는 아니다. 현재 API 계약은 `code: "session_expired"`로 구분한다.
- 기록 공개 범위에서 `is_private=false`는 친구 열람 가능 상태이며, 인터넷 전체에 자동 공개한다는 뜻이 아니다. 외부 링크는 별도의 명시적 공유 상태(`shared_at`)를 확인한다.
- Next PWA/Workbox, Vercel 배포를 사용한다. 알림 기능 구현과 실제 자동 발송 활성화 상태는 다르므로 현재 운영 문서와 설정을 별도로 확인한다.

현재 저장소 명령:

```sh
npm ci
npm run dev       # next dev --webpack
npm test          # vitest run
npm run lint     # eslint .
npm run build    # next build --webpack
```

문서만 바꿨다면 링크·경로·서술 정확성과 포맷을 확인한다. 실행 코드 변경은 관련 테스트와 저장소 검사로 확인한다. 환경값이 필요한 검증의 결과를 환경 없이 통과했다고 보고하지 않는다.

## 근거 문서

- [노션 Readiary](https://app.notion.com/p/3c6eedeede86804b9ac8fdf7ca4b7698)
- [노션 문장 중심 개편 계획](https://app.notion.com/p/3c6eedeede86817fbecad5f826e030f0)
- [노션 결정 로그](https://app.notion.com/p/f5d2f26fcc7d4640a8e798e9cac161a4)
- [UI Guidelines](docs/ui-guidelines.md)
- [문장 중심 설계](docs/superpowers/specs/2026-08-25-sentence-centric-redesign-design.md)
- [페이지 로딩과 데이터 접근](docs/operations/page-loading-performance.md)
- [개인정보·공개 범위 운영 기록](docs/operations/privacy-release.md)
- [Web Push 운영 안내](docs/notifications/web-push.md)

위 품질·운영 규칙 다섯 가지는 사용자 승인으로 반영되었다. UI 검토 보고서의 개선 후보는 별도의 구현 범위이며 자동으로 적용하지 않는다.
